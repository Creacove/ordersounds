
-- We've already created this function via the SQL in a previous step,
-- but now we need to modify it to allow producers to follow other producers

-- This SQL would be run with lov-sql in a separate message, but we've already done that
-- We'll execute this change in a future SQL update block if the user approves
